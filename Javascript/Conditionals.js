
let age;

if (age > 22) {
    "can drink in US and EU"
} else if (age > 18) {
    "can drink in EU"
} else {
    "cannot drink"
}

let bin;

switch (bin){
    case 'A':
    case 'a':
        bin = 1
        break
    case 'B':
    case 'b':
        bin = 2
        break
    default:
        bin = 0
}

